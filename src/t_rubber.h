// Rubber for prepared strings// �2006 Thomas Resch// www.zippernoise.net// ported to c from Stefan Belbaos's Prepared Piano																									/*																									    *//*-------------------------------------------struct t_rubber--------------------------------------------*//*																										*/#include "t_std.h"typedef struct _t_rubber{ double nextRubber; double rubber; double lastRubber; //double *rubberForce;    double rubberForce[MAXNUMBEROFSTRINGS]; double rubberMassDensityRatio;  // Rubber/String... double rubberFrequency; // fundamentalFreqRubber //double *rubberPosition;    double rubberPosition[MAXNUMBEROFSTRINGS]; double dt; double dt_2; double dt_over2; double dx; double normRubberIndex; int rubberIndex; int rubberOn; int NS; double rubberLoss; double w_2;  double rubberLoss_mul_dt_over2_plus1;  double one_minus_rubberLoss_mul_dt_over2;  } t_rubber;t_rubber *t_rubber_new(double rubberFrequency_, double rubberMassDensityRatio_, double rubberLoss, int NS_);void t_rubber_free(t_rubber *x);void t_rubber_perform(t_rubber *x, double ***nextString, double ***string);void t_rubber_set_dxdt(t_rubber *x, double dx_, double dt_);void t_rubber_setRubberParameters(t_rubber *x, double fundamentalFreq, double massDensityRatio, double rubberLoss);void t_rubber_calculateRubberIndex(t_rubber *x, double normRubberIndex_);void t_rubber_reset(t_rubber *x);